
/* gets() and string allocation example
 */
#include cxstdio

int main () {

    puts("Please enter your name: ");
    char *in = gets();

    char *greet = "Hello ";
    greet += in;
    greet += ", yous a bad mother-fucker!";
    puts(greet);

    return 0;
}