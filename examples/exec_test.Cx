
int global_int = 500;

int sqr_root(int number){
    int root = number;

    //global_int = 2;

    do
        root = (number/root + root)/2;
    while((root * root) > number);

    return root;
}

int main(){

    int root = sqr_root(4096);

    root = global_int;
    return root;
}
