
int sqr_root(int number){
    int rt = number;

    do
        rt = (number/rt + rt)/2;
    while((rt * rt) > number);

    return rt;
}

int main(){
    int i = 20;
    int root = sqr_root(4096);

    return i;
}