
int test;

{

    int number;
    int root;

    number = 4096;
    root = 4096;

    int i = 0;

    do{
        if(i == 10) break;

        root = (number/root + root)/2;
        i++;
    }while(root * root > number);

    //return root;
}