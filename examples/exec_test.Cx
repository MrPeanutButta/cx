int main(){

    int number = 4096;
    int root = number;
    int i = 5;

    do{
        root = i;
        if(i == 10) break;

        root = number + 5;
        i++;
    }while(root * root > number);

    //return root;
}