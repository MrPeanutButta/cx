

int main(){
    // hex test
    int x = 0xffff;
    int y = 0xabcdef;
    int a = x << 3;

    // octal test, should == 83 in decimal
    int oct = 0123;

    // binary test, should == 83 in decimal
    int bin = 0b01010011;

    // lets make sure octal does not fuck with float values starting in 0.0
    float flo = 5.5;

    //__cx_stdout__ = a;

    return 0;
}
