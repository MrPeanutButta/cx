#include stdio

int main () {

	file infile;
	char *buffer = "test";
	stdout.puts(buffer);
	
	if(!infile.open("textfile", "r"))
		puts("error opening file");
	else {
	stdout.puts("\ntesttesttest\n");
		buffer = infile.read(1,3);
		infile.close();
		
		
	}

    return 0;
}