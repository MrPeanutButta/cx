
/* standard type member functions example
 * 
 * var.to_str() - returns the string copy of the value of var
 * var.each(type var) - iterator for each element
 */

#include stdio

int main () {
	int sveta = 31;
	puts(sveta.to_str);

    return 0;
}