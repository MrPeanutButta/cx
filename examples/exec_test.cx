
/* gets() and string allocation example
 */

#include cxctype
#include cxstdio
//

int main () {

    char *mf = '!' + "beep" + "-boop" + '!';
    char *mft =  mf + mf;
    
    puts(mft + '!' + "another append" + gets() + "--------------------");
    puts("!" + "!");

    return 0;
}