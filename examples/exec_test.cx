#include stdio

int main () {

	//file outfile;
	char *buffer = "test";
	puts(buffer);
	
	/*if(!outfile.open("textfile", "r"))
		puts("error opening file");
	else {
	
		outfile.puts(buffer);
	}*/

    return 0;
}