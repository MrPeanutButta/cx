
/* gets() and string allocation example
 */

#include cxctype
#include cxstdio
//

int main () {

    char *mf = '!' + "mutha" + "-fucka" + '!';
    char *mft =  mf + mf;
    
    puts(mft + '!' + "another append" + gets() + "----dont be an asshole");
    puts("!" + "!");

    return 0;
}